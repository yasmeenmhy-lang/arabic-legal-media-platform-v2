// ★ بقرار مالكة المنصة (٢٠٢٦-٠٧-٢٨): حُذفت آلية «الصياغة المقترحة التلقائية»
// (كانت هنا: buildGovernedRewriteSuggestions) — كانت تُستدعى تلقائياً مع كل
// مراجعة فيها ملاحظة، تستهلك نداء ذكاء اصطناعي فعلياً بتكلفة حقيقية، ونتيجتها
// (governedRewrites) لم تكن تُعرض في أي صفحة من صفحات المنصة إطلاقاً — تحقق
// شامل من كل مستهلكيها أثبت ذلك. حُذفت الآلية كاملة لوقف الصرف بلا فائدة؛
// مسار إعادة الصياغة الوحيد الآن هو الزر اليدوي «صياغة مقترحة» (app/api/reformulate).

export function getRecommendations() {
  return [];
}
